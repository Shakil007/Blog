%h1= @article.title
=image_tag @article.image.url.to_s
%p= @article.content

= link_to "Edit", edit_article_path(@article)
= link_to "Destroy", article_path(@article), method: :delete, data: { confirm: "Are you sure?" }
= link_to "Home", root_path